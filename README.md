# 7ai-web

基于 Next.js App Router 的 Web 应用。当前 `0.0.1` 为四页面静态占位（首页 / 登录 / 对话 / 控制台），不含业务逻辑与接口。

## 开发

```bash
npm install
npm run dev
```

## 脚本

- `npm run dev` — 本地开发
- `npm run build` — 生产构建
- `npm run start` — 启动生产服务
- `npm run lint` — ESLint

## 迭代文档

见 `iterations/0.0.1/` 下各阶段产物。