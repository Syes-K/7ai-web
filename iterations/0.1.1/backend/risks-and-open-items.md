# 风险与开放项：助手管理（version 0.1.1）

## 1. 产品/范围

| ID | 描述 | 缓解 |
| --- | --- | --- |
| P1 | 对话页与助手绑定（默认助手、URL 带 `assistantId`）未在 PRD 强制 | 3B 可先完成数据与控制台/管理 UI；聊天集成单独立项。 |
| P2 | 系统助手删除后，用户侧收藏/偏好若引用该 id | 本期无「用户偏好指向助手」字段则无迁移；若后续增加须约定级联或清空策略。 |

---

## 2. 技术

| ID | 描述 | 缓解 |
| --- | --- | --- |
| T1 | `tags` 存储为 JSON 数组时的查询与索引 | MVP 用应用层过滤或简单 LIKE；量大再评估。 |
| T2 | 列表返回完整 `prompt` 的 payload 体积 | 可加分页 + 详情接口；列表截断需在 API 明确。 |

---

## 3. 安全

| ID | 描述 | 缓解 |
| --- | --- | --- |
| S1 | 控制台用户伪造 id 调用 PATCH | 服务端校验 `scope` 与 `userId`，禁止修改系统与他人个人记录。 |
| S2 | 管理接口暴露 | 全程 `withAdminApi`，与现有 admin 一致。 |

---

## 4. 修订记录

| 日期 | 说明 |
| --- | --- |
| 2026-04-13 | 初稿 |
