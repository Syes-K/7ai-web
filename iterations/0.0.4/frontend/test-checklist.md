# 前端自测清单：提示词模版（0.0.4）

- [ ] 侧栏与页头文案为「提示词模版」，路由仍为 `/admin/prompts`。
- [ ] 已登录访问 `/admin/prompts`：加载后展示与默认常量一致的表单项（无 JSON 文件时）；每项 **支持参数** 以 Tag 展示，hover 有 Tooltip（与 `params` 描述一致）。
- [ ] `name` / Tooltip / `value` 与 GET 数据一致；空 `desc` 无问号图标。
- [ ] 修改 `value` 后保存：刷新或再进入仍为最新（以磁盘为准）。
- [ ] 在正文中写入未声明占位符（如 `{unknown}`）或非法 `{`：保存前表单校验失败，且与后端错误语义一致。
- [ ] 损坏 `data/promptConfig.json`：出现警告 Alert，表单仍为默认文案；保存后警告消失且文件合法。
- [ ] 清空某 `value` 点保存：前端校验阻止或后端 400 有提示。
- [ ] 未登录直接请求 API：401；从管理页会话过期时跳转登录。

## 修订记录

| 日期 | 说明 |
| --- | --- |
| 2026-04-11 | 同步：文案、参数 Tag/Tooltip、`{参数}` 校验用例 |
