# 与设计/PRD 偏差说明（0.1.2 · 前端）

## 本迭代明确记录的差异

1. **侧栏时间格式（设计 §5.2）**  
   列表项时间已改为固定 **`YY-MM-DD HH:mm`**（与设计 §5.2 中「相对/绝对混排」示例可能不同）；若需改回相对时间文案，可再迭代。

2. **控制台链权限**  
   空列表时「去助手管理创建」指向 `/console/assistants`。设计写明无权限时可隐藏；当前未做运行时权限探测，**始终渲染链接**。无权限用户将得到控制台 403/重定向（与站点全局策略一致）。

---

其余 **§2～§3**（Modal 加载/空/错、`assistantUnavailable` 列表与消息区处理）按设计实现；无额外 intentional 偏离。
